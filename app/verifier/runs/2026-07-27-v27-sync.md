# v27 同步用户仓库更新 验证记录

## 用户新增内容
- L1「宜居地带」无限模式：`game/infiniteL1.ts`（区段变体：天鹰/跃金/哥特/衔尾/花园/维护通廊）+ `game/infiniteRegistry.ts`（多无限层级注册表，防循环依赖）；`levels/l1.ts` 改 `infinite: true`，出口改为楼梯井/未上锁的门（均 →L2）；`levels/l0.ts` 新增罕见出口「向下的灰色阶梯」→L1（配返程阶梯机制）
- 现象系统 `game/phenomena.ts`：孤立效应 / 植殖癌 / 闪烁，HUD 顶部现象图标条，dev 面板 phenOn/phenOff 强制开关
- 档案系统 `game/docs.ts` + `components/DocOverlay.tsx`（M.E.G. 内部资料）；笔记本 `components/NotebookOverlay.tsx`（notebook 物品使用触发，字迹自动保留）
- `public/favicon.svg`、`public/textures/manila_wallpaper.png`（geometry.ts 换用真实米色锦缎墙纸，`textureUrl()` 支持显式扩展名）
- HUD 物品扩展（v32：cashew/knife/axe/headlamp/notebook/fuyouyu/squirtgun/warpberry/royalration 配色与图标）、App overlay 新增 notebook/doc

## 防回归核查（用户版均保留，无需从 master 恢复）
- v24 `engine.devTestField()` + HUD「测试场地」按钮 ✓
- v25 entities habitat/aquatic 标记、mapgen/infinite 栖息地过滤、HUD 15+ 新物品 SVG 分支与 GLYPH_COLOR ✓
- v26 `manilaWallTexture()`（作为真实贴图的程序化兜底保留）、0.56 实体间距、桌顶 groundHeight、frontdesk 真实轮廓、ladder 细柱、fixHanging、ceilingHeightAt 天花板碰撞 ✓

## 我方修复（用户版缺陷）
1. **infiniteL1 实体放置未做栖息地过滤**（注释声称有过滤但代码没有；carrier 等 outdoor 实体生成在室内且未计数）→ 按 v25 契约实现 tryPick(hab) + 降级 any + `habFallback` 计数；`GenChunk`/`LiveChunk` 增加 `habFallback` 字段，stitch 时并入 `m.habitatFallback`；infinite.ts L0 路径同步改为计数（原仅 console.warn）
2. **两张引用贴图缺失导致部署 404**：`exit_sign_v1`、`grass_tuft_v1` → 按 src 程序化兜底同代码用浏览器 canvas 逐像素导出 `public/textures/exit_sign_v1.png` / `grass_tuft_v1.png`（grass 需 alpha 通道故 PNG），调用点改为显式 `.png` 扩展名

## 断言更新（用户新功能导致过时）
- check.mts「L0 唯一出口=闪烁门→L1」→ 放宽为「主出口=闪烁门→L1 且所有出口 →L1」（用户新增灰色阶梯出口，有意设计）

## 验证结果
- `npx tsc --noEmit` ✓；`npm run build` ✓
- check.mts：433 通过 / 0 失败
- smoke-v25 / smoke-v26：全部通过
- smoke-v27（新增）：L1 无限 chunk 生成/确定性/窗口平移缝合（25→25 chunk、实体无越界）、超区域出口生成、DOCS 完整性、notebook 事件、三现象触发与理智流失 —— 全部通过
- Playwright（1280×800 + iPhone 13，dist 静态服务）：L1 无限场景（初始+移动后）、档案覆盖层、笔记本覆盖层截图于 `verifier/runs/shots-v27/`；console 0 报错、HTTP≥400 为 0（favicon/马尼拉墙纸/新贴图均无 404）
